## Releases

- Use Conventional Commits for `cli/` changes because Release Please derives versions and release notes from them.
- CLI release commits must touch `cli/**` so the release workflow runs.
